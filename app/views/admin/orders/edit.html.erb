<% content_for :page_title, "案件編集" %>

<div class="flex flex-col gap-4">
  <h2 class="text-xl font-bold text-slate-900">案件編集</h2>
  <%= render "form", order: @order, url: admin_order_path(@order) %>
</div>
