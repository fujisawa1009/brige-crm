<div class="flex flex-col gap-4">
  <h1 class="text-2xl font-bold"><%= @customer_status.label %></h1>
  <dl class="grid grid-cols-[max-content_1fr] gap-x-4 gap-y-1 text-sm">
    <dt class="font-bold">コード</dt><dd><%= @customer_status.code %></dd>
    <dt class="font-bold">システム定義</dt><dd><%= @customer_status.is_system %></dd>
    <dt class="font-bold">有効</dt><dd><%= @customer_status.is_active %></dd>
  </dl>
  <div class="flex gap-3">
    <%= link_to "一覧に戻る", admin_customer_statuses_path, class: "underline" %>
    <% if policy(@customer_status).update? %><%= link_to "編集", edit_admin_customer_status_path(@customer_status), class: "underline" %><% end %>
    <% if policy(@customer_status).destroy? %>
      <%= button_to "削除", admin_customer_status_path(@customer_status), method: :delete, form: { data: { turbo_confirm: "削除しますか？" } } %>
    <% end %>
  </div>
</div>
