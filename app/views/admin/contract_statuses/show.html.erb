<% content_for :page_title, "契約ステータス詳細" %>

<div class="flex flex-col gap-6">
  <div class="flex items-center justify-between">
    <div>
      <h2 class="text-xl font-bold text-slate-900"><%= @contract_status.label %></h2>
      <p class="text-sm text-slate-500 font-mono"><%= @contract_status.code %></p>
    </div>
    <div class="flex gap-2">
      <%= link_to "一覧に戻る", admin_contract_statuses_path,
            class: "btn-secondary" %>
      <% if policy(@contract_status).update? %>
        <%= link_to "編集", edit_admin_contract_status_path(@contract_status),
              class: "btn-primary" %>
      <% end %>
    </div>
  </div>

  <div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">
    <h3 class="mb-4 text-sm font-semibold uppercase tracking-wide text-slate-500">基本情報</h3>
    <dl class="grid grid-cols-1 gap-4 sm:grid-cols-2">
      <div>
        <dt class="text-xs text-slate-500">表示順</dt>
        <dd class="mt-1 text-slate-800"><%= @contract_status.sort_order %></dd>
      </div>
      <div>
        <dt class="text-xs text-slate-500">状態</dt>
        <dd class="mt-1 flex gap-1">
          <% if @contract_status.is_system? %>
            <%= badge_tag "組み込み（コード変更不可）", color: "slate" %>
          <% end %>
          <%= active_badge(@contract_status.is_active?) %>
        </dd>
      </div>
    </dl>
  </div>

  <% if !@contract_status.is_system? && policy(@contract_status).destroy? %>
    <div>
      <%= button_to "このステータスを削除", admin_contract_status_path(@contract_status), method: :delete,
            form: { data: { turbo_confirm: "#{@contract_status.label} を削除しますか？" } },
            class: "btn-danger" %>
    </div>
  <% end %>
</div>
