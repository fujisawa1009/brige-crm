<% content_for :page_title, "フォームテンプレート編集" %>

<div class="flex flex-col gap-4">
  <h2 class="text-xl font-bold text-slate-900">フォームテンプレート編集</h2>
  <%= render "form", form_template: @form_template, url: admin_form_template_path(@form_template) %>
</div>
