# 機能非機能要件・追加要望 統合インベントリ

> Rails版改訂: 2026-08-19。旧Laravelプロジェクト（boilerplate-vue-env/laravel/requirements/design/legacy-research/04-requirements-inventory.md）を brige-crm（Rails 8.1）の現行実装・03/04 の決定に合わせて見直し。フェーズ対応: 全フェーズ横断（機能64件の実装状況表）。要件64件・要望の調査事実は不変。「状態」「対応フェーズ」欄を **brige-crm の実装状況（R0〜R4 実装済み／R5・R6 未実装）**に更新し、削除済み `remaining-tasks.md` への参照を差し替えた。旧 P 番号は併記のまま残す。
>
> 出典: `機能非機能要件_20260417.xlsx`（機能64件）/ 大平さん要件 / TBSS要望（會川さん）
> 位置づけ: `basic-design.md`（18章）の元ネタ。**実装済みと未実装のギャップを可視化**する。

---

## 1. 機能非機能要件 64件の実装状況

`basic-design.md` は主にログイン・顧客・申込・契約フローをカバーするが、
**要件64件には basic-design に載っていない運用機能が多数含まれる**。

### 1-1. basic-design.md でカバー済み（概ね実装 or 計画済み）

| 項番 | 機能 | 旧Laravel時点の状態 | brige-crm 状態（2026-08-19 突合） |
|---|---|---|---|
| 1-6 | 顧客一覧・詳細・編集・利用停止 | 実装済み（CRUD） | **R2 実装済み**（`Admin::CustomersController`、`Customer.active`／退会 = `customer_statuses` withdrawn、Pundit スコープ適用） |
| 7 | 申込登録 | P2 | **R3 実装済み**（`Form::*` 営業担当者ログイン＋メールOTP、`FormTemplate/FormStep/FormField` 動的マルチステップ、`Application` → Customer/Store/Order 一括生成） |
| 8 | 入力チェック設定 | P3-10 | **R5 未実装**（3段階必須。`FormField.validation_rules` の基本バリデーションのみ R3 で実装済み） |
| 9-11 | 不備チェック・差戻し・確認コール | P3-5〜7 | **R5 未実装**（契約ワークフロー状態機械。受け皿カラム `orders.confirm_call_*` は R2 実装済み） |
| 12-13 | 契約書作成・参照 | P3-8〜9 | **R5 未実装**（PDF生成・版数管理・手書き署名） |
| 14 | 案件検索（メール条件追加） | P4-6 | **未実装（R6）**。現行 `Admin::OrdersController` の検索は `order_number` ILIKE ＋ status のみ、`Admin::CustomersController` は name / customer_number のみ |
| 15-19, 31-32 | ログイン管理（個別アカウント・認証・セッション） | 実装済み | **R0 実装済み**（Devise ＋ ftlog式メールOTP `OtpAuthenticatable`、rack-attack、`IpAllowlistEntry`。全画面2FA = Q-23 は R3 form / R4 mypage にも展開済み） |
| 20-24 | 権限管理・参照範囲制御 | 一部実装／P4-1 | **R0/R1 実装済み**（エンドポイントRBAC `SystemPermission`/`SystemRole` ＋ 権限マトリクスUI、参照範囲制御 = Pundit `policy_scope`。旧Laravelで未実装だった P4-1 は解消） |
| 25-28 | ユーザ管理 | 実装済み | **R1 実装済み**（`Admin::UsersController` ＋ CSV一括アップロード `UserCsvImportJob`） |
| 29-30 | 監査ログ | 実装済み／P4-16 | **R0 実装済み**（`Auditable` concern → `AuditLog`。ログイン履歴は `Admin::LoginHistoriesController` = AuditLog の絞り込みビュー） |
| 59-63 | 問い合わせ・通知管理 | 実装済み／P4-13,14 | **R4 実装済み**（`Inquiry`/`InquiryMessage`/`Notification`/`SystemNotification`/`RecipientGroup`/`NotificationTemplate`）。**問い合わせ返信テンプレート（FAQ 12カテゴリ・差し込み変数・選択UI）は未実装**（04 R4 ギャップ・`13-faq-templates.md`） |

### 1-2. ⚠️ basic-design.md に無い（＝計画から抜けている）運用機能

**これらは `development-plan.md` に追加済み（P4-18〜24 / P5-13）。** Rails 版 04 では R6（運用強化）に集約され、2026-08-19 時点で**すべて未実装**。P5-13 は別トラック。

| 項番 | 機能 | カテゴリ | 現行の代替 | 追加先 |
|---|---|---|---|---|
| 34-35 | GBPアカウント権限管理・受注完了経過管理 | BridgePlus管理 | 【#BridgePlus】管理シート（スプシ） | P4-23 |
| 36 | 初回運用レクチャー管理 | 運用 | スプレッドシート | P4-18近辺 |
| 37, 47-48 | **月次レポート作成・送付管理** | 運用 | 手動メール（アシスト50万/月） | P5-13（別意思決定） |
| 38 | KW（キーワード）管理 | 運用 | スプシ | P4-23近辺 |
| 39-41 | **遅延案件 検知・通知・一覧**（バッチ） | 遅延案件管理 | スプシ手動確認＋S-RAPO | P4-18 |
| 42 | **滞留案件ポップアップ表示**（ログイン時） | ログイン後表示 | なし | P4-18 |
| 43-45 | **自動キャンセル**（判定・代理店通知・自動実行） | バッチ | 手作業 | P4-19 |
| 46 | 代理店管理画面での資料共有 | 代理店 | — | 一部（営業資料） |
| 49-50 | **集計機能**（実績集計・Excel出力） | 集計 | 手作業 | P4-20 |
| 51-52 | **連携状況記録・連携エラー通知メール**（バッチ） | 連携管理 | PAD自動化Excel（20260702） | P4-21 |
| 53-56 | **外部CSV取込**（root / GBP / OBIC7） | CSV取込 | 手作業 | P4-22 |
| 57-58 | ステップ配信（解約抑止メール） | 配信 | — | 検討段階 |
| 64 | **対応音声ログ管理** | 音声ログ | なし（現行の課題） | P4-24 |

> **重要な気づき**：現行の「Excel・スプレッドシート・PAD自動化」でしのいでいる業務が
> 要件として多数上がっている。これらは `development-plan.md` の P4（業務運用機能）に
> **新規タスク P4-18〜24 として追加済み**（→ 04 R6。R6 は「要件ごとに個別判断」のため、着手順の優先度は §4 表を参照）。

---

## 2. 大平さんの追加要望（現場起点の網羅リスト）

§1-2 と重複するが、**現場の温度感**が読み取れる。特に強い要望：

| 要望 | 対応フェーズ（旧P → R） | 補足 | brige-crm 状態 |
|---|---|---|---|
| 各工程の誤入力検知（リリース後も管理者調整可） | P3-10 → R5 | 「ルール変更に追従できること」が肝 | 未実装（`FormField.validation_rules` はフォームビルダーから調整可＝R3 済み。工程別チェックは R5） |
| 案件検索へメールアドレス条件 | P4-6 → R6 | — | 未実装 |
| 社内ログインを個別メールアドレスへ | 実装済み | 現行はチーム共通アカウント | R0 実装済み（Devise `users.email` 一意） |
| スプレッドシート機能の取り込み（管理シート・運用状況） | P4-20/23 → R6 | GBP権限管理・経過管理・レポート送付管理・KW管理 | 未実装 |
| **遅延案件の検知・通知・一覧化**（日数は管理者調整） | P4-18 → R6 | 通知はメール自動送付 | 未実装（Solid Queue recurring で実装想定） |
| **ログイン時ポップアップ**（自分の滞留案件一覧） | P4-18 → R6 | 案件番号・店舗名・滞留場所・ステータス | 未実装 |
| **自動キャンセル**（期限超過→通知→キャンセル/クローズ） | P4-19 → R6 | キャンセル=サイン済／クローズ=サイン無し | 未実装（E8 通知先は未決。04 R4/R6 注記） |
| 月次レポート作成・送付 | P5-13（別トラック） | 顧客マイページ・案件詳細から確認 | 未実装・要否未決 |
| GBPアカウント管理 | P4-23 → R6 | | 未実装（`order_work_details.gbp_*` の受け皿カラムは R2 済み） |
| 集計機能（受注/計上/解約/遅延、期間/代理店/担当別） | P4-20 → R6 | ヒートマップ・グラフ・Excel出力 | 未実装 |
| **連携状況の記録＋連携エラー時の自動アウトバウンドメール** | P4-21 → R6 | いつから連携エラーか一目で分かるように | 未実装 |
| 外部CSV取込（root/GBP/OBIC7） | P4-22 → R6 | rootはAPI困難のためCSV | 未実装（CSV基盤は `CsvExport`/`UserCsvImportJob` のみ） |
| ステップ配信（解約抑止） | 検討 | | 未実装・要否未決 |
| 問い合わせ対応・記録・テンプレート | 実装済み／P4-13,14 → R4 | | 対応・記録は R4 実装済み。**返信テンプレート（FAQ）は未実装** |
| 代理店通達・顧客一斉通知 | P4-8 → R4 | | R4 実装済み（`Notification` フィルタ・スケジュール送信・`NotificationDeliveryJob`） |
| **対応音声ログの格納** | P4-24 → R6 | クレーム対応のため顧客ログを掌握 | 未実装 |
| 顧客による契約書入力フロー | P2／方針⑥ → R5 | 現行は営業が代理入力 | 未実装（R3 は営業担当者ログイン前提。`01` §2-1 #4 参照） |

---

## 3. TBSS要望（會川さん・請求まわり）

旧 `remaining-tasks.md` 7章と一致（同ファイルは 2026-08-19 に削除済み・旧Laravel側に残存。7-1 の要点は 04 R2 追加タスク「未収情報フィールドの追加要否」として拾い上げ済み）。案件テーブル（`orders`）への追加フィールド。

| # | 要望 | 対応（旧） | brige-crm 状態（2026-08-19 突合） |
|---|---|---|---|
| ① | 未収情報（売上伝票番号・売上年月・請求先名・未回収額・未回収額合計） | P4-11 | `orders.sales_mgmt_slip_number`（売上伝票番号）のみ R2 実装済み。売上年月・請求先名・未回収額・未回収額合計は**未実装**。R2 追加カラムか R6（集計・請求）かを要否から判断（04 R2 追加タスク） |
| ② | お纏め請求情報（おまとめ番号・案件間リンク） | P4-11 | `orders.bundled_billing` / `bundle_target_order_number`（文字列）R2 実装済み。案件間の FK リンク化は未実装（`02` C-n 論点13 と連動・R5） |
| ③ | 備考情報（外部リンク対応。ガルーンのリンク等） | P4-11 | `orders.remarks` / `shared_notes`（text）R2 実装済み。リンクの表示対応は未実装（R6） |
| ④ | 予備欄（テキスト形式・多め） | P4-11 | **未実装**（要否含め R6 で判断） |

---

## 4. development-plan.md への反映（追加済みタスク）

P4 に運用機能グループ、P5 に月次レポートを追加済み。Rails 版 04 ではいずれも **R6（運用強化）**に属し未実装（バッチは Solid Queue recurring／rake タスクで実装する想定。Excel出力は Ruby 標準 `csv` ＋ `CsvExportJob` の延長で CSV から検討）。

| タスク | 機能 | 優先度 | Rails版 |
|---|---|---|---|
| P4-18 | 遅延案件の検知・通知・一覧（バッチ＋ログイン時ポップアップ） | 高（現場要望強い） | R6 未実装 |
| P4-19 | 自動キャンセル/クローズ（判定・通知・実行バッチ） | 高 | R6 未実装（E8 通知先未決） |
| P4-20 | 集計・レポーティング（実績集計・Excel出力） | 中 | R6 未実装 |
| P4-21 | 連携状況記録＋連携エラー自動アウトバウンド | 中 | R6 未実装 |
| P4-22 | 外部CSV取込（root/GBP/OBIC7） | 中 | R6 未実装 |
| P4-23 | GBPアカウント権限管理・経過管理（管理シートの取込） | 中 | R6 未実装 |
| P4-24 | 対応音声ログ管理 | 中（容量・法務要確認） | R6 未実装 |
| P4-25 | ガルーン/サイボウズ連携（`03` §2-2・`06` §3。情シス確認必須） | 中 | R6 未実装 |
| P5-13 | 月次レポート作成・送付（別意思決定と連動） | 別トラック | 未決 |

---

## 5. 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-07-24 | 初版。機能64件の実装状況を突合し、basic-design/development-plan から抜けている運用機能を特定。P4-18〜24・P5-13 を提案・追加 |
| 2026-08-19 | **Rails版改訂**（brige-crm）。機能64件・要望・TBSS要望の各表に brige-crm 実装状況列を追加（R0〜R4 実装済み／R5・R6 未実装の突合結果）。`remaining-tasks.md` 削除済みの差し替え。P4-25 を §4 表に追加 |
