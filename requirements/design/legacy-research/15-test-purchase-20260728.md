# テスト購入 記録ログ（Step 1・2026-07-28 実施）

> 目的: 現行ジャスミン（BridgePlus / ネットムーブ S084）で実際にテスト購入を行い、
> **①新旧APIどちらか**（`/checkout` か `/cgi-bin/get_back.pl` か）
> **②1桁問題**（`jutyu_cd` 下位と `member_id` の実関係）
> **③戻りパラメータの実物**（ret_url に何が返るか）を確定する。
> 実施者: CEO。記録: 秘書（本ファイルへ逐一追記）。
>
> ⚠️ **PIIルール**: card_num / cvv / 有効期限は**記録しない**（マスク必須）。
> HMACキー等の秘密値も値そのものは書かない（存在と所在のみ記録）。
> 参照: `../pii-handling-rules.md`
>
> **Rails版改訂: 2026-08-19。** 旧Laravelプロジェクト（`boilerplate-vue-env/laravel/requirements/design/legacy-research/15-test-purchase-20260728.md`）を
> brige-crm（Rails 8.1）へ集約。**フェーズ対応: R5（契約フロー・決済。ネットムーブ連携）**。本記録は現行（旧ベンダー）システムでのテスト購入ログであり内容は不変。
> - 状態: タイムライン・送信パラメータ・戻り記録は **未記入のまま**（2026-08-19 時点でも Step 1 の実施結果は本ファイルに追記されていない）。R5 着手前チェックリスト Q-37（jutyu_cd 桁数）・Q-38（決済結果の確定手段）の一次確認手段として、実施済みなら結果を本ファイルへ追記すること。
> - 削除済み参照の差し替え: `../../impl-plans/P3-2-payment.md`（削除済み・旧Laravel側に残存）→ `../04-implementation-plan.md` R5（決済状態機械・request spec 必須）。`_NEXT.md`（旧Laravel側の作業メモ）→ `04-implementation-plan.md`「次のアクション」。

---

## 0. 確認したいこと（チェックリスト）

| # | 確認項目 | 共有してもらうもの | 状態 |
|---|---|---|---|
| 1 | 支払方法の選択画面 | 画面キャプチャ | ⬜ |
| 2 | カード入力画面（**アドレスバー込み**＝ドメイン/パス確認） | 画面キャプチャ | ⬜ |
| 3 | 送信パラメータ（F12 Network タブ。**card_num / cvv は隠す**） | パラメータ一覧 | ⬜ |
| 4 | 完了画面＋戻り先URL（**アドレスバー込み**） | 画面キャプチャ | ⬜ |
| 5 | （終了後）管理画面で**与信取消**＝後始末 | 実施報告 | ⬜ |

### この結果で確定させたい論点

- **新旧API判定**: カード入力画面のURLが `/checkout` なら導入ガイド Ver 1.0.5 の新方式、
  `/cgi-bin/get_back.pl` なら現行旧方式のまま。
- **1桁問題**: 送信された `jutyu_cd`（受注コード）と、管理画面に記録される会員IDの実関係。
  （既知: 会員ID `60001864`(8桁) vs 受注コード下位 `6001864`(7桁)。CRMエクスポートでは全件「6＋7桁」）
- **戻りパラメータ**: ret_url に処理結果コードが付くか（ガイド記載では返らない・I-18）。
- **3Dセキュア**: 認証フローが実際に挟まるか（2025年10月必須化済みのはず）。
- **check_cd**: 送信パラメータに含まれるか・形式（前任メモでは加盟店側生成）。

---

## 1. タイムライン（逐一追記）

> 形式: `### HH:MM 見出し` ＋ 共有内容の要約・転記・所見。

（開始待ち — CEOからの共有を受け次第ここへ追記）

---

## 2. 送信パラメータ記録（#3 受領後に転記）

| パラメータ名 | 値（PIIはマスク） | 所見 |
|---|---|---|
| （受領待ち） | | |

---

## 3. 戻り（ret_url）記録（#4 受領後に転記）

- 戻り先URL:
- クエリ/POSTパラメータ:
- 完了画面の表示内容:

---

## 4. 判明事項まとめ（テスト終了後に確定）

| # | 論点 | 結果 | 反映先 |
|---|---|---|---|
| A | 新旧API判定 | （未確定） | `02-payment-netmove.md` |
| B | 1桁問題（jutyu_cd ⇄ member_id） | （未確定） | `../netmove-card-migration.md` §2-4 |
| C | 戻りパラメータの実物 | （未確定） | `02-payment-netmove.md` §5-b・`../payment-integration.md` §4-9 |
| D | 3Dセキュアの実挙動 | （未確定） | `../04-implementation-plan.md` R5（旧 `impl-plans/P3-2-payment.md` は削除済み） |
| E | check_cd の形式 | （未確定） | `02-payment-netmove.md` |

---

## 5. 後始末

- ⬜ 管理画面「取引履歴」で当該取引を確認
- ⬜ **与信取消**を実施（商用カード・実環境のため必須）
- ⬜ 取消後の取引状態を記録（取引状態7種のどれになるか → `../payment-integration.md` §4-4 の対応表検証）

---

## 6. 反映タスク（テスト終了後）

1. 本ファイル §4 の確定内容を `02-payment-netmove.md` / `netmove-card-migration.md` へ反映
2. 必要なら `payment-integration.md`（戻り受け口設計）・`04-implementation-plan.md` R5（テスト観点。旧 `P3-2-payment.md` は削除済み）を更新
3. ~~`_NEXT.md` の Step 1 を完了に更新~~ → `04-implementation-plan.md` R5着手前チェックリスト（Q-37/Q-38）に結果を反映
