# 残り資料の調査（クロージング）

> 作成日: 2026-07-24
> 位置づけ: 未読だった資料を読み切り、**資料調査フェーズを完結**する。
> 対象：解約抑止フロー / 業務割振り / 卸対応・再連携マニュアル / 會川さんMoc / 決済ガイド後半。
>
> **Rails版改訂: 2026-08-19。** 旧Laravelプロジェクト（`boilerplate-vue-env/laravel/requirements/design/legacy-research/14-remaining-materials.md`）を
> brige-crm（Rails 8.1）の 03/04 の決定に合わせて見直し。**フェーズ対応: 旧 P3-2 → R5（決済）、旧 P4-14/18/19/21 → R4/R6（通知・遅延検知・自動キャンセル・連携記録）、旧 P5-13 → R6/R8。**
> - 調査事実（資料の内容・新規発見・読めない資料の一覧）は改変していない。追記したのは各「新システムでの対応」に対応する R フェーズと実装状況のみ。
> - 実装状況（2026-08-19）: `payment_transactions`（§3 クレカNG自動追跡の前提）は **R5 未着手**。P4-21 連携エラー記録・P4-18 滞留検知・P5-13 月次レポートは **R6 未着手**。P4-14 通知は R4 実装済み（`SystemNotification` / `NotificationTemplate` / `RecipientResolver`）。

---

## 0. 結論：資料調査は完了。新規発見は少数（既知フローの補足が中心）

残り資料の大半は、`business-flow-analysis`・`06-bw-operations` で把握済みの業務の
**担当者割当・画像手順書・外部ツールの補足**。以下に新規発見のみ記録する。

---

## 1. 解約抑止フロー（新規：外部ツール TimeRex）

`【BridgePlus】解約抑止依頼フロー20260423.xlsx`。**解約希望顧客の引き止め（面談）業務**。

```
解約希望 → TimeRex（外部日程調整ツール）で受付
  → 1.日程選択 2.情報登録 3.日程調整完了 4.お客様へメール 5.ガルーンへ共有
    → 解約/キャンセル依頼スレッド（cybozu）で共有
      → アシスト様（システム対応）が対応 → 報告確認
```

| 新規ツール | 用途 |
|---|---|
| **TimeRex**（`timerex.net`） | 解約抑止面談の日程調整（Zoom面談予約） |

> **P4-19（自動キャンセル。Rails版 R6）とは別業務**。P4-19 は「進行が滞った案件の自動キャンセル/クローズ」、
> 本フローは「解約したい顧客を面談で引き止める」引き止め業務。新システムでの扱いは要検討
> （日程調整は外部ツール継続の可能性が高い＝代替不可カテゴリ）。R フェーズ未割当（R6 で要否判断）。

---

## 2. 業務割振り（BW担当者マップ）

`00_【割振り】引継ぎ業務振り分け.xlsx`。BW担当者ごとの業務一覧。
既知業務（掲示板・問い合わせ・卸発注・解約・GBP権限・管理シート）の担当者割当が中心。

**新規に明示された業務**：

| 業務 | 内容 | 新システムでの対応 | Rails版フェーズ・実装状況（2026-08-19） |
|---|---|---|---|
| **連携エラー案件の抽出・共有**（毎日・PAD自動化） | CSVを走らせて連携エラーを抽出しチームスで共有 | P4-21（連携状況記録＋自動通知） | R6・未実装 |
| **クレカNG顧客の集計**（毎日） | 會川さん更新のExcelを反映、クレカNG案件を管理 | **決済状態で自動追跡**（P3-2。§3参照） | R5・未実装（`payment_transactions` 未作成） |
| 月次レポート担当者コメント作成 | 毎月約100件のコメント作成 | P5-13（月次レポート） | R6/R8・未実装 |
| 未対応スレッドの催促 | 部署横断で止まっているスレッドをチェック | P4-14（通知・滞留検知 P4-18） | 通知基盤は R4 実装済み（`SystemNotification`・Solid Cable）。滞留検知（P4-18）は R6・未実装 |

---

## 3. ★ クレカNG顧客管理（決済との連動・新規論点）

業務割振りに「**クレカNG顧客の集計**（毎日・Excel）」が登場。
= **決済登録に失敗した顧客をExcelで手動集計・管理**している。

| 現行 | 新システム | Rails版フェーズ |
|---|---|---|
| クレカNG顧客を會川さんがExcel更新→BWが反映 | `payment_transactions.status`（failed / unknown）で**自動追跡** | R5（決済状態機械。未実装。`payment-integration.md` §4-4） |
| 手動でアウトバウンドメール（PAD自動化） | 決済失敗→自動通知/下書き（P4-21 と同型） | R5/R6（通知マトリクス E6「決済失敗の通知先」は未決＝04 R5着手前チェックリスト） |

> **決済連携（P3-2）の副産物として「クレカNG顧客一覧」が自動で得られる**。
> `payment-integration.md` §4-4 の `failed`/`unknown` 状態がこの管理を置き換える。
> → `payment-integration.md` / P3-2（Rails版 R5）に「クレカNG顧客の自動追跡・通知」を追記すべき。

---

## 4. 読み切れなかった資料（画像・専用形式・実装時精読）

| 資料 | 状況 | 扱い |
|---|---|---|
| 卸対応マニュアル（5シート） | **画像埋め込み**（テキスト抽出不可）。シート名＝インポート〜発注/受注/納品後/解約/差し戻し | 内容は `business-flow-analysis`・`06` で把握済み。画像詳細は実装時に原本参照 |
| 再連携マニュアル | ほぼ空（画像のみ） | シート名＝FTWEBアカウントによる再連携。手順は原本参照 |
| 會川さんMoc（pptx / fmp12） | pptx 1スライド（画像のみ）／fmp12 は FileMaker | 要望のモック。TBSS要望（`04` §3）で把握済み |
| 決済ガイド p11-23 | 未精読 | **P3-2（Rails版 R5）実装時に精読**（パラメータ全詳細・処理結果コード・テスト方法） |

> これらは**テキストでは読めない/実装時参照でよい**もの。資料調査としては、
> フロー・要件レベルの把握は完了している。

---

## 5. 外部ツールの全リスト（更新）

`business-flow-analysis` §2 に **TimeRex** を追加した完全版：

| # | ツール | 提供元 | 新システムでの扱い |
|---|---|---|---|
| 1-12 | ジャスミン/電子契約/発注フォーム/Forms/root/S-RAPO/infoBiz/Synup/Teams/スプシ/解約フォーム/ガルーン | （既出） | `business-flow-analysis` §8 |
| 13 | **TimeRex** | 外部 | 解約抑止の日程調整。代替不可（外部日程ツール） |

---

## 6. 反映事項

| 反映先 | 内容 | Rails版フェーズ |
|---|---|---|
| `payment-integration.md` / P3-2 | クレカNG顧客の自動追跡・通知（現行はExcel手動集計） | R5 |
| `business-flow-analysis.md` §2 | 外部ツールに TimeRex を追加 | — |
| `development-plan.md` P4-19 | 解約抑止（引き止め）は自動キャンセルと別業務。TimeRex継続の可能性 | R6（`04-rails-implementation-plan.md` R6 の遅延案件検知・自動キャンセルと切り分け） |
| `04-requirements-inventory.md` | 連携エラー抽出（PAD）＝P4-21 の裏付け | R6 |

---

## 7. 資料調査フェーズ 完了宣言

`legacy-research/00〜14` で、`private/ジャスミン資料/` の主要資料を**読み切った**。

- テキスト/表形式の資料：全て精読・ノート化
- 画像/FileMaker/決済ガイド後半：フロー把握済み、詳細は実装時に原本参照
- 検証：数値・固有名詞・破損・ギャップを複数パスで確認（誤り検出・修正済み）

→ **調査は完了**。以降は設計を実装単位に落とす／実装フェーズへ移行する段階（Rails版: R0〜R4 実装済み・R5 以降は `04-rails-implementation-plan.md`）。

---

## 8. 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-07-24 | 初版。残り資料を読み切り資料調査を完結。新規発見（TimeRex／クレカNG顧客の決済連動／連携エラーPAD）を記録。読めない資料（画像/FileMaker/決済後半）の扱いを明示 |
| 2026-08-19 | Rails版改訂。各「新システムでの対応」に R フェーズ（R4 実装済み／R5・R6 未実装）を付記。調査内容は不変 |
